<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:LoraWeather.ViewModels;assembly=LoraWeather"
             x:Class="LoraWeather.Views.PickerTestView">
    
    <ContentPage.BindingContext>
        <viewModels:PickerTestViewModel/>
    </ContentPage.BindingContext>
    <StackLayout Orientation="Horizontal" >
        <Label Text="Data: "/>
        <Picker Title="Data"  ItemsSource="{Binding Items}" SelectedIndex="{Binding SelectedIndex}"/>
    </StackLayout>
    
    
</ContentPage>